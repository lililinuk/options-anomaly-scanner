# Analytics boundary

Future derived analytics live here after raw ingestion and vendor-neutral normalization. The hierarchy is contract → expiry → strike structure → ticker positioning → lifecycle → price/GEX/IV confirmation → tradeability. No scoring, anomaly threshold, opening/closing inference, or directional recommendation is implemented in Phase 1.

